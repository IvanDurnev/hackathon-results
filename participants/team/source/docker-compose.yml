version: "3.9"

services:
  api:
    build: .
    container_name: ai-ppt-generator
    ports:
      - "8000:8000"
    env_file:
      - .env               # ваш .env с токеном и прочими параметрами
    volumes:
      - ./tmp/cache:/app/tmp/cache   # сохраняем кеш между рестартами
