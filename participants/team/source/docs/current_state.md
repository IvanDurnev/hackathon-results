# Текущее состояние MVP

Проект реализован как компактный in-memory прототип на стандартной библиотеке Python.

## Структура

```text
C:/Users/User/lol/
  app.py
  README.md
  static/
    index.html
    app.js
    styles.css
  case/
    1_RCB/
    2_Agreements/
    3_StateTask/
    4_BUAU_Export/
  docs/
    rag/
  tests/
```

## Backend

`app.py` загружает данные из `case/`, нормализует записи, фильтрует их, агрегирует суммы, сравнивает периоды и отдаёт JSON API через `ThreadingHTTPServer`.

Добавлены:

- `GET /api/catalog/quick-actions` для быстрых сценариев;
- `POST /api/assistant` для rule-based и опционального Groq-помощника;
- mini-RAG по markdown-документам из `docs/rag`;
- человекочитаемый `human_summary` в `/api/trace`.

## Frontend

Vue 3 UI через CDN работает без сборки. Первый экран теперь начинается с быстрых действий, умной строки, assistant-блока и короткого вывода. Расширенные фильтры сохранены, но визуально вторичны.

Добавлены:

- быстрый старт;
- smart suggestions без LLM;
- короткий вывод над результатом;
- понятная таблица со статусами;
- хорошие пустые состояния;
- проверка готовности демонстрации;
- экспорт с названием "Скачать таблицу".

## RAG

`docs/rag/*.md` содержит короткие объяснения терминов, шаблонов, метрик, источников, сценариев и ограничений. Retrieval простой: совпадения слов в локальных markdown-файлах, без векторной БД.

## Assistant

Assistant не считает суммы и не меняет данные. Он выбирает режим, шаблон, метрики и поисковый текст. Финансовый результат всегда строится существующими backend-функциями.

## Ограничения

- Нет БД и миграций.
- Нет фонового импорта.
- Нет векторной базы.
- Groq опционален; без ключа работает fallback по правилам.
